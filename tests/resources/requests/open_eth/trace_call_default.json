{
  "id": 2,
  "jsonrpc": "2.0",
  "method":"trace_call",
  "params":[
      {
          "from": "{{from}}",
          "to": "{{to}}",
          "data": "{{input}}",
          "gas": "{{gas}}",
          "gasPrice": "{{gasPrice}}",
          "nonce": "{{nonce}}"
      },
      ["trace", "vmTrace","stateDiff"], "latest"
    ]
}